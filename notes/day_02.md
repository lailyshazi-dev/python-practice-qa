# День 02: pytest и параметризация

## Что я сделал

- Создал новые функции в калькуляторе и написал тесты для них.
- Отладил некоторые ошибки и разобрался с импортом функции из другого файла.
- Сделал некоторые тесты вызовом функции сразу для нескольких значений.

## Что понял

- Понял некоторые новые моменты синтаксиса касательно оформления функций.
- Понял момент с оформлением кода: между функциями нужно оставлять две пустые строки.
- Понял, как работает файловая система.

## Что было сложно

- Все вообще легко, но нужно уточнить, как работает функция, которую я вызвал извне для тестов: `@pytest.mark.parametrize`.

## Новые слова

- `import` - импортировать функцию или объект из другого файла.
- `assert` - проверка ожидаемого результата.
- `parametrize` - запуск одного теста с разными данными.

